[package]
name = "math"
version = "0.1.0"
edition = "2021"

[dependencies]
glam = "0.30.5"
serde = { version = "1", features = ["derive"], optional = true }

[features]
default = ["serialization"]
serialization = ["serde", "glam/serde"]
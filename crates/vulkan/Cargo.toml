[package]
name = "vulkan"
version = "0.1.0"
edition = "2024"

[dependencies]
libloading = "0.9.0"
planitia-workspace-hack = { version = "0.1", path = "../../planitia-workspace-hack" }

[features]
debug = []
format_info = []

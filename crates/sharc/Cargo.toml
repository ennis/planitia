[package]
name = "sharc"
version = "0.1.0"
edition = "2024"

[dependencies]
utils = { workspace = true }
gpu-types = { workspace = true }
log = "0.4.28"
planitia-workspace-hack = { version = "0.1", path = "../../planitia-workspace-hack" }

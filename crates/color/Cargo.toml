[package]
name = "color"
version = "0.1.0"
edition = "2024"

[dependencies]
gpu = { workspace = true, optional = true }
serde = { version = "1", features = ["derive"], optional = true }

[features]
default = ["gpu-support", "serialization"]
gpu-support = ["dep:gpu"]
serialization = ["dep:serde"]
[package]
name = "color"
version = "0.1.0"
edition = "2024"

[dependencies]
math = { workspace = true }
serde = { version = "1", features = ["derive"], optional = true }

[features]
default = ["serialization"]
serialization = ["dep:serde"]